import { Helmet } from "react-helmet";

const Menu = () => {
    return (
        <div>
           <Helmet>
            <title>SunShine || Menu</title>          
           </Helmet>
         this is menu
        </div>
    );
};

export default Menu;